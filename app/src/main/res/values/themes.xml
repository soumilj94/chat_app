<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BranchChatApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/colorPrimaryLight</item>
    </style>

    <style name="Theme.BranchChatApp" parent="Base.Theme.BranchChatApp" />

    <style name="ChatCard" parent="CardView">
        <item name="android:backgroundTint">@color/colorPrimaryLight</item>
    </style>

    <style name="ChatText" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>